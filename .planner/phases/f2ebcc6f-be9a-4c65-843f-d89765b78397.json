{
  "id": "f2ebcc6f-be9a-4c65-843f-d89765b78397",
  "featureId": "061d1d51-2801-4966-b02f-1ecb9f0dd1cf",
  "number": 2,
  "shortId": "UYJSM",
  "priority": 1,
  "slug": "rule-model-evaluation-and-precedence",
  "title": "Rule model, evaluation and precedence",
  "discussedAt": "",
  "contextReady": false,
  "contextReadyReason": "",
  "summary": "The rule types, the evaluator, and most-specific-wins",
  "description": "CURRENT STATE: nothing built. No Rust crate exists yet; this phase creates it.\n\nDefine the rule model and the evaluator that both platform shells call. Serves R2.\n\nRULE MODEL: each rule is deny or allow, and one of exact number, pattern, starts-with, ends-with, prefix, caller ID, or location (city/region). Location is NOT a separate matcher — libphonenumber geocoding maps place to prefix, so a location rule resolves to a prefix rule. One implementation, per the recorded decision.\n\nEVALUATOR: evaluate(E164, RuleSet) -> Allow | Block | Silence | Label(text). Called live on Android calls, Android SMS and iOS SMS. Must be fast and allocation-light — it runs inside an iOS extension under memory pressure and on the call path.\n\nPRECEDENCE — MOST SPECIFIC WINS: the longer, more precise rule beats the broader one, so allow on an exact number beats deny on the range containing it, with no manual ordering. Implement as longest-match, the way a routing table does. Equally specific conflicting rules must be REPORTED, not silently resolved — surface them to the UI so the user decides.\n\nEDGE CASES: overlapping denies of different specificity; an allow with no enclosing deny (meaningful on Android, a no-op on iOS call expansion — the evaluator should be honest about this rather than pretend); rules written against a country whose metadata later changes; an empty rule set; a rule matching every number.\n\nCONSTRAINTS: no country, prefix or length in code (Guidelines §1). Rules are the persisted truth; expansions never are (Guidelines §2).\n\nTESTABILITY: pure logic, no platform dependency — fully testable on the host. A bug here is a bug on every platform simultaneously, so this phase should carry the heaviest test coverage in the project.",
  "descriptionUpdatedAt": "2026-09-08T08:29:52.822Z",
  "notes": "",
  "goals": [],
  "nonGoals": [],
  "dependencies": [],
  "dependsOn": [],
  "risks": [],
  "openQuestions": [],
  "decisions": [],
  "acceptedDecisions": [],
  "completionCriteria": [],
  "taskIds": [
    "e5b60bdf-d792-4f26-bd99-d0f062afddb5",
    "810e41af-a580-4785-8bed-05aea8a1122f"
  ],
  "tasks": [
    {
      "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5",
      "phaseId": "f2ebcc6f-be9a-4c65-843f-d89765b78397",
      "number": 4,
      "shortId": "ZADF9",
      "priority": 1,
      "shortName": "rule-model-and-specificity-ran",
      "title": "Rule model and specificity ranking",
      "status": "done",
      "description": "CURRENT STATE. core/src/lib.rs holds only core_version(), default_entry_limit(), EntryLimit and a Decision enum (Allow/Block/Silence/Label per the phase description — verify the variants against the file before extending). No rule type exists. The crate builds three ways (lib/staticlib/cdylib, core/Cargo.toml:12) and is exposed over UniFFI.\n\nGOAL. Define the persisted rule model and the total specificity order the evaluator will use. No evaluation logic here — that is T002.\n\nWHAT TO BUILD, in core/src/rule.rs (new), re-exported from lib.rs:\n\n1. Effect: Deny | Allow. Every rule carries one.\n\n2. RuleKind, the seven kinds from R2: ExactNumber, Pattern, StartsWith, EndsWith, Prefix, CallerId, Location. Location is NOT a distinct matcher — per the recorded decision it resolves to Prefix via geocoding (P004). Model it so that resolution happens before evaluation and the evaluator never sees a Location variant; an enum split between \"authored\" and \"resolved\" forms is the obvious shape, but pick what keeps the evaluator total.\n\n3. Rule: effect, kind, and whatever the kind needs. Plus a stable id — the UI must point at \"the rule that did not fit on iPhone\" (R2) and at both sides of a conflict.\n\n4. RuleSet: the collection the evaluator takes. Give it whatever index makes longest-match cheap; do not sort at evaluation time.\n\n5. Specificity: a total order across kinds and within a kind, so \"most specific wins\" is decidable without user ordering. Longer beats shorter, the way a routing table resolves. Two rules can tie — that is legal and T002 reports it.\n\n6. Conflict detection: find pairs of equally-specific rules with opposing effects that can both match. R2 requires these be surfaced, never resolved silently.\n\nCONSTRAINTS.\n- Guidelines §1: no country, prefix, number length or place name anywhere in this file. Specificity must be derived from the rule, not from knowledge of any numbering plan.\n- Guidelines §2: this is the persisted truth. Nothing here stores or caches an expansion.\n- Guidelines §4: no measured value as a literal.\n- Runs inside an iOS extension under memory pressure and on the Android call path with a 5-second budget, so keep it allocation-light. No per-evaluation allocation in the types this task defines.\n\nEDGE CASES to encode as tests: empty RuleSet; a rule matching every number; two identical rules with opposite effects; an Allow with no enclosing Deny (legal, and a no-op on iOS call expansion — the model must not forbid it); overlapping Denies of different specificity; a Pattern and a StartsWith that match the same number.\n\nDONE WHEN. Types compile, specificity is a proven total order, conflict detection is tested both ways (finds a real conflict, and does not fire on a genuine specificity difference), and cargo test / fmt / clippy -D warnings / tools/check-no-hardcoded-numbering.sh all pass.\n\n---\n**Completion summary:**\nSHIPPED. Commit ea287ef.\n\ncore/src/rule.rs (new, ~430 lines) and core/tests/rule_model.rs (new, 18 tests). core/src/lib.rs re-exports the public names.\n\nTYPES. Effect (Deny/Allow); RuleId(u64), assigned by storage, never invented by the core; Digits, a validated digit run with separators stripped and non-digits refused rather than dropped; Pattern, positional Atom::Digit/Atom::Any, wildcard `x`, refusing a pattern that pins nothing; Matcher with five variants (Exact, StartsWith, EndsWith, Pattern, CallerId); Authored with three (Direct, Prefix, Location); Rule; RuleSet; Specificity; Conflict; PrefixSource.\n\nDECISION — authored wraps resolved, it does not duplicate it. R2 lists seven rule kinds but several match identically: a dialling prefix and a starts-with both fix leading digits, and a location resolves to a prefix. Two parallel seven-variant enums would have meant writing the same match twice. Authored::Direct(Matcher) plus Prefix and Location gives seven authored kinds over five matchers, one implementation of leading-digit matching, and an evaluator that never sees a Location. Authored::resolve(&dyn PrefixSource) does the mapping; PrefixSource is the seam P004(F002) implements.\n\nDECISION — specificity is counted, not ranked by kind. Specificity { pinned: u16, fixes_length: bool }, Ord derived in that field order. Exact and Pattern fix the length; StartsWith and EndsWith do not; CallerId pins zero digits and therefore ranks below a one-digit prefix. Consequence worth knowing: a Pattern and a StartsWith pinning the same digits can never tie, because the pattern also fixes length. Nothing here knows what a country is, so nothing could rank kinds on anything but digits pinned.\n\nDECISION — overlap detection errs toward reporting. Matcher::can_overlap returns true unless disjointness is provable. Proving two rules disjoint generally needs each country's number lengths, which Guidelines §1 keeps out of the core. R2 forbids resolving an equally-specific conflict silently, and a conflict never noticed is exactly that, so a false report the user dismisses is the safe direction and a missed one is not. StartsWith-vs-EndsWith is the case this affects: always reported.\n\nRuleSet::new sorts descending by specificity, id as tiebreak so the order is stable across launches. RuleSet::conflicts() scans only within contiguous equal-specificity runs, and is documented as off the call path.\n\nDECISION — number-shaped fixtures live in core/tests/, not core/src. The §1 lint refuses digit runs of four or more anywhere under core/src, and that strictness is worth keeping. An integration test compiles to its own binary and ships with nothing, so those fixtures are not numbering data compiled into the core. Unit tests that remain in rule.rs use three-digit runs and test mechanics, not numbers.\n\nVERIFIED. cargo test: 27 pass (9 unit, 18 integration), 0 fail. cargo fmt --check clean. cargo clippy --all-targets -D warnings clean. tools/check-no-hardcoded-numbering.sh passes. UniFFI binding generation re-run against the changed lib.rs and still succeeds. All on the host; no device involved and none needed — this module is pure logic.\n\nCORRECTED DURING THE WORK. A first test asserted a Pattern and a same-width StartsWith would tie and conflict. They cannot, for the reason above; the test was wrong, not the code. Replaced with a pattern-vs-pattern conflict test plus direct can_overlap tests.\n\nNOT DONE HERE, deliberately. No matching and no evaluate() — that is T005. The cross-kind arms of can_overlap (Pattern vs StartsWith/EndsWith, StartsWith vs EndsWith) are unreachable through conflicts() because those kinds cannot tie; can_overlap is a general public predicate and P003's allow-subtraction is the likely next caller, so it is kept general and tested directly rather than trimmed to what conflicts() reaches. Nothing is exported over UniFFI yet; the FFI surface is P005(F002).\n\nUNVERIFIED. Nothing in this module has run on a device or been called from Swift or Kotlin. Allocation-freedom on the matching path is a property of the types, not yet measured, because there is no matching path until T005.",
      "descriptionUpdatedAt": "2026-09-09T08:58:42.192Z",
      "notes": "",
      "statusLog": [
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-001-planned-in-progress",
          "date": "2026-09-09T08:53:02.595Z",
          "fromStatus": "planned",
          "toStatus": "in-progress",
          "title": "planned → in-progress",
          "description": ""
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-002-in-progress-done",
          "date": "2026-09-09T08:58:42.191Z",
          "fromStatus": "in-progress",
          "toStatus": "done",
          "title": "in-progress → done",
          "description": "SHIPPED. Commit ea287ef.\n\ncore/src/rule.rs (new, ~430 lines) and core/tests/rule_model.rs (new, 18 tests). core/src/lib.rs re-exports the public names.\n\nTYPES. Effect (Deny/Allow); RuleId(u64), assigned by storage, never invented by the core; Digits, a validated digit run with separators stripped and non-digits refused rather than dropped; Pattern, positional Atom::Digit/Atom::Any, wildcard `x`, refusing a pattern that pins nothing; Matcher with five variants (Exact, StartsWith, EndsWith, Pattern, CallerId); Authored with three (Direct, Prefix, Location); Rule; RuleSet; Specificity; Conflict; PrefixSource.\n\nDECISION — authored wraps resolved, it does not duplicate it. R2 lists seven rule kinds but several match identically: a dialling prefix and a starts-with both fix leading digits, and a location resolves to a prefix. Two parallel seven-variant enums would have meant writing the same match twice. Authored::Direct(Matcher) plus Prefix and Location gives seven authored kinds over five matchers, one implementation of leading-digit matching, and an evaluator that never sees a Location. Authored::resolve(&dyn PrefixSource) does the mapping; PrefixSource is the seam P004(F002) implements.\n\nDECISION — specificity is counted, not ranked by kind. Specificity { pinned: u16, fixes_length: bool }, Ord derived in that field order. Exact and Pattern fix the length; StartsWith and EndsWith do not; CallerId pins zero digits and therefore ranks below a one-digit prefix. Consequence worth knowing: a Pattern and a StartsWith pinning the same digits can never tie, because the pattern also fixes length. Nothing here knows what a country is, so nothing could rank kinds on anything but digits pinned.\n\nDECISION — overlap detection errs toward reporting. Matcher::can_overlap returns true unless disjointness is provable. Proving two rules disjoint generally needs each country's number lengths, which Guidelines §1 keeps out of the core. R2 forbids resolving an equally-specific conflict silently, and a conflict never noticed is exactly that, so a false report the user dismisses is the safe direction and a missed one is not. StartsWith-vs-EndsWith is the case this affects: always reported.\n\nRuleSet::new sorts descending by specificity, id as tiebreak so the order is stable across launches. RuleSet::conflicts() scans only within contiguous equal-specificity runs, and is documented as off the call path.\n\nDECISION — number-shaped fixtures live in core/tests/, not core/src. The §1 lint refuses digit runs of four or more anywhere under core/src, and that strictness is worth keeping. An integration test compiles to its own binary and ships with nothing, so those fixtures are not numbering data compiled into the core. Unit tests that remain in rule.rs use three-digit runs and test mechanics, not numbers.\n\nVERIFIED. cargo test: 27 pass (9 unit, 18 integration), 0 fail. cargo fmt --check clean. cargo clippy --all-targets -D warnings clean. tools/check-no-hardcoded-numbering.sh passes. UniFFI binding generation re-run against the changed lib.rs and still succeeds. All on the host; no device involved and none needed — this module is pure logic.\n\nCORRECTED DURING THE WORK. A first test asserted a Pattern and a same-width StartsWith would tie and conflict. They cannot, for the reason above; the test was wrong, not the code. Replaced with a pattern-vs-pattern conflict test plus direct can_overlap tests.\n\nNOT DONE HERE, deliberately. No matching and no evaluate() — that is T005. The cross-kind arms of can_overlap (Pattern vs StartsWith/EndsWith, StartsWith vs EndsWith) are unreachable through conflicts() because those kinds cannot tie; can_overlap is a general public predicate and P003's allow-subtraction is the likely next caller, so it is kept general and tested directly rather than trimmed to what conflicts() reaches. Nothing is exported over UniFFI yet; the FFI surface is P005(F002).\n\nUNVERIFIED. Nothing in this module has run on a device or been called from Swift or Kotlin. Allocation-freedom on the matching path is a property of the types, not yet measured, because there is no matching path until T005."
        }
      ],
      "decisions": [],
      "acceptedDecisions": [],
      "checklist": [
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-001-read-core-src-lib-rs-and-confirm-the-existing-decision-variants",
          "number": 1,
          "title": "Read core/src/lib.rs and confirm the existing Decision variants",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-002-effect-and-rulekind-covering-all-seven-r2-kinds",
          "number": 2,
          "title": "Effect and RuleKind covering all seven R2 kinds",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-003-rule-with-a-stable-id-the-ui-can-point-at",
          "number": 3,
          "title": "Rule with a stable id the UI can point at",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-004-ruleset-with-an-index-that-makes-longest-match-cheap",
          "number": 4,
          "title": "RuleSet with an index that makes longest-match cheap",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-005-location-modelled-so-it-resolves-to-prefix-before-evaluation",
          "number": 5,
          "title": "Location modelled so it resolves to Prefix before evaluation",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-006-total-specificity-order-ties-allowed",
          "number": 6,
          "title": "Total specificity order, ties allowed",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-007-conflict-detection-for-equally-specific-opposing-rules",
          "number": 7,
          "title": "Conflict detection for equally-specific opposing rules",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-008-tests-for-every-edge-case-listed-in-the-description",
          "number": 8,
          "title": "Tests for every edge case listed in the description",
          "checked": true
        },
        {
          "id": "e5b60bdf-d792-4f26-bd99-d0f062afddb5-check-009-cargo-test-fmt-clippy-d-warnings-no-hardcoded-numbering-lint",
          "number": 9,
          "title": "cargo test, fmt, clippy -D warnings, no-hardcoded-numbering lint",
          "checked": true
        }
      ],
      "subtasks": [],
      "dependsOn": [],
      "pauseSnapshot": null,
      "pauseHistory": [],
      "startedAt": "2026-09-09T08:53:02.595Z",
      "completedAt": "2026-09-09T08:58:42.191Z",
      "createdAt": "2026-09-09T08:42:11.824Z",
      "updatedAt": "2026-09-09T08:58:42.191Z",
      "sessionInfo": [
        {
          "sessionId": "mcp-process:24483334-5f42-42eb-9b01-2f39eceecde9",
          "createdAt": "2026-09-09T08:53:02.567Z"
        }
      ]
    },
    {
      "id": "810e41af-a580-4785-8bed-05aea8a1122f",
      "phaseId": "f2ebcc6f-be9a-4c65-843f-d89765b78397",
      "number": 5,
      "shortId": "SD4SM",
      "priority": 2,
      "shortName": "evaluator-with-most-specific-w",
      "title": "Evaluator with most-specific-wins precedence",
      "status": "done",
      "description": "DEPENDS ON P002(F002)/T004 — the rule model and specificity order.\n\nCURRENT STATE. No evaluator exists. core/src/lib.rs has a Decision enum; check its variants before use. T004 will have added core/src/rule.rs with Effect, RuleKind, Rule, RuleSet and a total specificity order.\n\nGOAL. evaluate(e164: &str, rules: &RuleSet) -> Decision, in core/src/evaluate.rs (new). This is the function both platform shells call, and per Guidelines §3 it is the only place filtering logic lives. Serves R2.\n\nWHAT TO BUILD:\n\n1. evaluate() returning Allow | Block | Silence | Label(text). Called live on Android calls, Android SMS and iOS SMS. Not called on iOS calls — those are pre-expanded (P003) — so this function must not assume it sees every decision the product makes.\n\n2. Most-specific-wins: the highest-specificity matching rule decides, regardless of authoring order. An Allow on an exact number beats a Deny on the range containing it. Implement as longest-match, not as an ordered scan with early exit.\n\n3. Equally-specific conflicts: when the winners tie with opposing effects, do not pick one. Return or surface the conflict so the UI can ask the user (R2). Decide deliberately whether evaluate() reports this inline or a separate query does, and record which in the completion note — the call path cannot afford to build conflict reports on every call.\n\n4. Input handling: evaluate takes E164. Normalization lives with parsing in P003, so this task takes already-normalized input and must say so at the boundary rather than half-normalizing.\n\nPERFORMANCE, both hard constraints from R2:\n- Android CallScreeningService must answer within 5 seconds of binding, and the phone does not ring until it does. Internal only, never user-visible.\n- The iOS SMS extension runs under memory pressure.\nTherefore: no allocation on the matching path, no regex compilation per call, no full scan of the rule set where the index from T004 can avoid it. Add a test that evaluates against a large rule set so a regression in complexity is visible.\n\nCONSTRAINTS.\n- Guidelines §1: no country, prefix, length or place name. The evaluator must not know what country a number is from.\n- Guidelines §3: no filtering logic in Swift or Kotlin; it all lands here.\n- Guidelines §4: no measured threshold as a literal.\n\nEDGE CASES to test: empty rule set (Allow); no rule matches (Allow); a rule matching every number; nested Deny/Allow/Deny at three specificities; two equally-specific opposing rules; an Allow with no enclosing Deny; a number that matches a Pattern and a StartsWith of equal specificity. Also: withheld numbers never reach us on Android (PRESENTATION_RESTRICTED and friends) — evaluate() should not be given a placeholder for them, and the boundary must make that impossible rather than relying on callers.\n\nDONE WHEN. evaluate() is correct against every case above, the large-rule-set test shows the index is actually used, and cargo test / fmt / clippy -D warnings / tools/check-no-hardcoded-numbering.sh all pass. Per the phase description this carries the heaviest test coverage in the project — a bug here is a bug on every platform at once.\n\n---\n**Completion summary:**\nSHIPPED. Commit fa7beee.\n\ncore/src/evaluate.rs (new) and core/tests/evaluate.rs (new, 17 tests). core/src/rule.rs gained Pattern::matches_str as pub. core/src/lib.rs re-exports evaluate, Call, Verdict, E164.\n\nAPI. evaluate(&Call, &RuleSet) -> Verdict { decision, matched: Option<&Rule>, contested: bool }. Verdict borrows, so it costs no allocation. `matched` names the rule that decided, which R2 needs — a rule must never silently start or stop applying, and the caller has to be able to say which one acted.\n\nDECISION — conflict reporting lives at edit time, not on the call path. This was the explicit open question in the task. RuleSet::conflicts() (T004) costs a pass over the rules and runs when rules are edited. evaluate() only sets a `contested` bool, which it already knows for free from the walk it is doing. Building a conflict report per call was never affordable.\n\nDECISION — a tie allows, and says so. When the best match ties with an opposing rule of equal specificity, evaluate does not pick a side on the merits: it returns Decision::Allow and sets contested. The phone is ringing, so something must happen now, and allowing is the recoverable half. An unwanted call that gets through is visible and can be blocked afterwards; a wanted call that is blocked is silent, and on iOS the system never tells the app it happened. The real resolution is asking the user at edit time.\n\nDECISION — E164 is the boundary that keeps withheld callers out. E164::new returns None for anything that is not a normalised number, including \"\", \"+\", \"unknown\" and \"private\". Android never delivers a withheld caller to a screening service (PRESENTATION_RESTRICTED, UNKNOWN, UNAVAILABLE, PAYPHONE), and there is deliberately no placeholder variant, so no caller can invent one and have it evaluated. It checks shape only; how long a number should be is country data and §1 keeps it out.\n\nCall carries the number plus an optional caller ID, because a call has both and a rule may match either. Caller ID is compared with eq_ignore_ascii_case — networks are inconsistent about case and a lowercased copy would allocate.\n\nPRECEDENCE. Walks the specificity-sorted set from most specific, stops as soon as it drops below the best match found, and while at that specificity checks only whether an opposing rule ties. Authoring order is irrelevant and there is a test asserting the same set gives the same answer reversed.\n\nVERIFIED. cargo test: 47 pass (12 unit, 17 evaluate, 18 rule_model), 0 fail. cargo fmt --check clean. cargo clippy --all-targets -D warnings clean. tools/check-no-hardcoded-numbering.sh passes. UniFFI binding generation re-run and still succeeds. Host only; this module is pure logic and needs no device.\n\nPERFORMANCE, and how far it was actually checked. The matching path allocates nothing and compiles no regex — every arm borrows. The large-set test is a tripwire, not a benchmark: 10,001 rules, 2,000 evaluations, asserted under a deliberately loose bound so a slow CI runner stays honest. It would catch the walk becoming quadratic; it would not catch a modest constant-factor regression. Note the index is only the sorted order, and a non-matching call still walks the whole set. That is fine and deliberate: real rule sets are tens of rules, because it is the iOS expansion that gets large, not the rule count. A trie would only pay at counts no user reaches.\n\nGAP FOUND, NOT FILLED. Decision has four variants; evaluate can only ever produce two. Decision::Silence and Decision::Label(String) are unreachable from the current rule model, because Effect is only Deny or Allow and carries no treatment. Label belongs to R3 (spam labelling, deferred, and iOS-calls-only, which never calls evaluate). Silence is a real Android capability (CallResponse.Builder setSilenceCall) that no rule can currently ask for. Filling this means either a per-rule treatment on a deny or a global user preference, and R2 specifies neither, so it was left alone rather than invented. Flagged for a product decision.\n\nUNVERIFIED. Nothing here has run on a device or been called from Swift or Kotlin. Allocation-freedom is a property of the code, argued and reviewed, not measured with an allocator counter. The five-second Android budget is not tested against — there is no Android runtime path yet.\n\nNOT DONE HERE. No normalisation: evaluate takes an already-normalised number and P003(F002) owns parsing. No expansion, no geocoding. Nothing exported over UniFFI yet; the FFI surface is P005(F002).",
      "descriptionUpdatedAt": "2026-09-09T16:04:40.153Z",
      "notes": "",
      "statusLog": [
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-001-planned-in-progress",
          "date": "2026-09-09T16:00:21.555Z",
          "fromStatus": "planned",
          "toStatus": "in-progress",
          "title": "planned → in-progress",
          "description": ""
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-002-in-progress-done",
          "date": "2026-09-09T16:04:40.152Z",
          "fromStatus": "in-progress",
          "toStatus": "done",
          "title": "in-progress → done",
          "description": "SHIPPED. Commit fa7beee.\n\ncore/src/evaluate.rs (new) and core/tests/evaluate.rs (new, 17 tests). core/src/rule.rs gained Pattern::matches_str as pub. core/src/lib.rs re-exports evaluate, Call, Verdict, E164.\n\nAPI. evaluate(&Call, &RuleSet) -> Verdict { decision, matched: Option<&Rule>, contested: bool }. Verdict borrows, so it costs no allocation. `matched` names the rule that decided, which R2 needs — a rule must never silently start or stop applying, and the caller has to be able to say which one acted.\n\nDECISION — conflict reporting lives at edit time, not on the call path. This was the explicit open question in the task. RuleSet::conflicts() (T004) costs a pass over the rules and runs when rules are edited. evaluate() only sets a `contested` bool, which it already knows for free from the walk it is doing. Building a conflict report per call was never affordable.\n\nDECISION — a tie allows, and says so. When the best match ties with an opposing rule of equal specificity, evaluate does not pick a side on the merits: it returns Decision::Allow and sets contested. The phone is ringing, so something must happen now, and allowing is the recoverable half. An unwanted call that gets through is visible and can be blocked afterwards; a wanted call that is blocked is silent, and on iOS the system never tells the app it happened. The real resolution is asking the user at edit time.\n\nDECISION — E164 is the boundary that keeps withheld callers out. E164::new returns None for anything that is not a normalised number, including \"\", \"+\", \"unknown\" and \"private\". Android never delivers a withheld caller to a screening service (PRESENTATION_RESTRICTED, UNKNOWN, UNAVAILABLE, PAYPHONE), and there is deliberately no placeholder variant, so no caller can invent one and have it evaluated. It checks shape only; how long a number should be is country data and §1 keeps it out.\n\nCall carries the number plus an optional caller ID, because a call has both and a rule may match either. Caller ID is compared with eq_ignore_ascii_case — networks are inconsistent about case and a lowercased copy would allocate.\n\nPRECEDENCE. Walks the specificity-sorted set from most specific, stops as soon as it drops below the best match found, and while at that specificity checks only whether an opposing rule ties. Authoring order is irrelevant and there is a test asserting the same set gives the same answer reversed.\n\nVERIFIED. cargo test: 47 pass (12 unit, 17 evaluate, 18 rule_model), 0 fail. cargo fmt --check clean. cargo clippy --all-targets -D warnings clean. tools/check-no-hardcoded-numbering.sh passes. UniFFI binding generation re-run and still succeeds. Host only; this module is pure logic and needs no device.\n\nPERFORMANCE, and how far it was actually checked. The matching path allocates nothing and compiles no regex — every arm borrows. The large-set test is a tripwire, not a benchmark: 10,001 rules, 2,000 evaluations, asserted under a deliberately loose bound so a slow CI runner stays honest. It would catch the walk becoming quadratic; it would not catch a modest constant-factor regression. Note the index is only the sorted order, and a non-matching call still walks the whole set. That is fine and deliberate: real rule sets are tens of rules, because it is the iOS expansion that gets large, not the rule count. A trie would only pay at counts no user reaches.\n\nGAP FOUND, NOT FILLED. Decision has four variants; evaluate can only ever produce two. Decision::Silence and Decision::Label(String) are unreachable from the current rule model, because Effect is only Deny or Allow and carries no treatment. Label belongs to R3 (spam labelling, deferred, and iOS-calls-only, which never calls evaluate). Silence is a real Android capability (CallResponse.Builder setSilenceCall) that no rule can currently ask for. Filling this means either a per-rule treatment on a deny or a global user preference, and R2 specifies neither, so it was left alone rather than invented. Flagged for a product decision.\n\nUNVERIFIED. Nothing here has run on a device or been called from Swift or Kotlin. Allocation-freedom is a property of the code, argued and reviewed, not measured with an allocator counter. The five-second Android budget is not tested against — there is no Android runtime path yet.\n\nNOT DONE HERE. No normalisation: evaluate takes an already-normalised number and P003(F002) owns parsing. No expansion, no geocoding. Nothing exported over UniFFI yet; the FFI surface is P005(F002)."
        }
      ],
      "decisions": [],
      "acceptedDecisions": [],
      "checklist": [
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-001-evaluate-e164-ruleset-decision-in-core-src-evaluate-rs",
          "number": 1,
          "title": "evaluate(e164, RuleSet) -> Decision in core/src/evaluate.rs",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-002-longest-match-precedence-not-an-ordered-scan",
          "number": 2,
          "title": "Longest-match precedence, not an ordered scan",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-003-tie-with-opposing-effects-surfaced-never-silently-resolved",
          "number": 3,
          "title": "Tie with opposing effects surfaced, never silently resolved",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-004-decide-and-record-where-conflict-reporting-lives-off-the-call-path",
          "number": 4,
          "title": "Decide and record where conflict reporting lives, off the call path",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-005-no-allocation-and-no-regex-compilation-on-the-matching-path",
          "number": 5,
          "title": "No allocation and no regex compilation on the matching path",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-006-large-rule-set-test-that-would-catch-a-complexity-regression",
          "number": 6,
          "title": "Large-rule-set test that would catch a complexity regression",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-007-tests-for-every-edge-case-listed-in-the-description",
          "number": 7,
          "title": "Tests for every edge case listed in the description",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-008-boundary-makes-a-withheld-placeholder-number-impossible-to-pass-in",
          "number": 8,
          "title": "Boundary makes a withheld/placeholder number impossible to pass in",
          "checked": true
        },
        {
          "id": "810e41af-a580-4785-8bed-05aea8a1122f-check-009-cargo-test-fmt-clippy-d-warnings-no-hardcoded-numbering-lint",
          "number": 9,
          "title": "cargo test, fmt, clippy -D warnings, no-hardcoded-numbering lint",
          "checked": true
        }
      ],
      "subtasks": [],
      "dependsOn": [],
      "pauseSnapshot": null,
      "pauseHistory": [],
      "startedAt": "2026-09-09T16:00:21.555Z",
      "completedAt": "2026-09-09T16:04:40.152Z",
      "createdAt": "2026-09-09T08:42:32.537Z",
      "updatedAt": "2026-09-09T16:04:40.152Z",
      "sessionInfo": [
        {
          "sessionId": "mcp-process:24483334-5f42-42eb-9b01-2f39eceecde9",
          "createdAt": "2026-09-09T16:00:21.529Z"
        }
      ]
    }
  ],
  "createdAt": "2026-09-08T08:29:52.818Z",
  "updatedAt": "2026-09-09T16:04:40.152Z",
  "handoff": "",
  "handoffUpdatedAt": "",
  "handoffAudit": null,
  "handoffReadAt": "",
  "handoffHistory": [],
  "statusLog": [
    {
      "id": "7c12a533-c79a-4219-9180-5b9bcec64c10",
      "date": "2026-09-09T08:53:02.600Z",
      "fromStatus": "draft",
      "toStatus": "in-progress",
      "title": "draft → in-progress",
      "description": ""
    },
    {
      "id": "28b76154-2868-4c32-8980-44e0c412cf3d",
      "date": "2026-09-09T16:04:40.158Z",
      "fromStatus": "in-progress",
      "toStatus": "done",
      "title": "in-progress → done",
      "description": ""
    }
  ],
  "sessionInfo": [
    {
      "sessionId": "mcp-process:24483334-5f42-42eb-9b01-2f39eceecde9",
      "createdAt": "2026-09-09T16:00:21.529Z"
    }
  ]
}