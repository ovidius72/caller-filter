[package]
name = "callerfilter-datasets"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Converts vendored libphonenumber data into the shipped dataset format."

[lib]
name = "callerfilter_datasets"
path = "src/lib.rs"

[[bin]]
name = "build-datasets"
path = "src/main.rs"

[dependencies]
callerfilter-core = { path = "../core" }
